<div class="o-table o-table--stacked">
    <table summary="Table Summary">
        <thead>
            <tr>
                {{#each headings}}
                    <th scope="col">{{heading}}</th>
                {{/each}}
            </tr>
        </thead>
        <tbody>
            {{#each rows}}
                <tr>
                    {{#each row}}
                        <td data-label="{{heading}}">{{data}}</td>
                    {{/each}}
                </tr>
            {{/each}}
        </tbody>
    </table>
</div>